#include "Jugador.h"

// TODO: Implementar la clase o las funciones del jugador cuando se desacople 
// la lógica de movimiento del main.cpp.

/* * EJEMPLO DE IMPLEMENTACIÓN FUTURA:
 *
 * /**
 * * @brief Actualiza la posición del jugador en la grilla validando colisiones.
 * * @param direccion El input de teclado (ej. KEY_UP, KEY_DOWN).
 * * @param mapaMatriz Referencia a la matriz del nivel actual.
 * * /
 * void MoverJugador(int direccion, int mapaMatriz[30][30]) {
 * // 1. Calcular próxima baldosa basada en la dirección.
 * // 2. Llamar a EsColision(x, y).
 * // 3. Si es falso, actualizar coordenadas X e Y del jugador.
 * // 4. Si pisa pasto alto, disparar evento de batalla.
 * }
 */